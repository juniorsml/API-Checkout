# Required metadata
sonar.projectKey=br.com.stelo.datamodel
sonar.projectName=Stelo Data Model
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=Interfaces

# Language
sonar.language=xml

# Encoding of the source files
sonar.sourceEncoding=UTF-8
